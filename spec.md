# Audio Thread Isolator (ATI) - 仕様書 (spec.md)

## 1. アプリケーション概要

- **名称**: 再生スレッドアイソレーター / Audio Thread Isolator (略称: ATI)
- **種別**: Windows ネイティブ常駐ユーティリティ（C++17 / Pure Win32 API / Comctl32）
- **目的**:
  オーディオ再生・動画・ブラウザアプリ等の実行中プロセスから「オーディオ再生スレッド（WASAPI / ASIO 等）」を自動走査・特定し、指定された単独の専用コア（例: `#3`）へ固定（Isolate）する。同時に、負荷の高いデコード・描画等の通常スレッド群を別コア（通常コア群）へ退避させることで、バッファアンダーラン（音途切れ）とジッターを極小化する。

---

## 2. 画面仕様・UI構成

### 2.1 メイン画面 (Main Window - 固定 8 列監視専用テーブル)
- **タイトル**: `Audio Thread Isolator`
- **フォント**: `Segoe UI` (9pt) - 高品位ネイティブレンダリング
- **プロセステーブル (ListView - 固定 8 列監視専用)**:
  1. **` ` (空カラム)**: 正方形インジケーター枠（幅 24, 中央揃え, ヘッダー空白, 最も左）
     - **● 青緑 (`#7CE0AD`)**: オーディオスレッド検出・専用コア隔離完了 ＋ 通常スレッド退避完了（正常隔離状態）。
     - **● コーラルピンク (`#E09B97`)**: 現在稼働中（スキャン中・待機中）。
     - **停止時**: 非表示（完全な空白、グレー丸は描画しない）。
     - **10% 明滅パルス**: ポーリング監視毎（500ms）に丸印の明度を約 10% 差で交互に明滅（パルス点滅）させ、フリーズせず常駐監視中であることを視覚提示。
  2. **`Process Name`**: 対象プロセス名（幅 105, 左揃え）
  3. **`For Audio`**: オーディオスレッド専有コア（幅 68, 中央揃え）
     - 単一コア時: `#5`、複数コア時: `#1,2`（プレフィックス先頭集約、カンマ連結）
     - 停止時: `-`
  4. **`Other than`**: 通常スレッド退避先コア（幅 75, 中央揃え）
     - システム全コアと実際に適用される通常スレッドマスクの差分（除外コア）を表示：
       - 未検知・スキャン中: `NormalCores` 設定で除外されたコアを表示（例: `#1` を空けている場合は `!#1`、全コア使用時は `All`）。
       - 隔離完了時: `NormalCores` の除外コアに加え、オーディオ専有コアも通常スレッドから除外された状態を表示（例: `#1` と `#5` が除外なら `!#1,5`）。
     - 停止時: `-`
  5. **`Audio Thread`**: 走査によって特定された再生スレッドの識別名（幅 125, 左揃え, カッコなし: `Scanning...` / `Not running`）
  6. **`Priority`**: オーディオスレッドの適用優先度（幅 75, 左揃え, 例: `Idle (-15)`）
  7. **`Threads`**: プロセス内のアフィニティ制御対象スレッド総数（幅 55, 中央揃え, 停止時は `-`）
  8. **`Changes`**: オーディオスレッドの累計隔離・変更回数（幅 60, 中央揃え, 初回起動時の文字切れ防止保護）
- **文字欠け防止 (動的ヘッダー幅自動計測と固定保護)**:
  - `LVSCW_AUTOSIZE_USEHEADER` を適用するが、固定幅の Col 0（24px）および Col 7（60px）は自動調整ループから除外し、初期起動時や高DPI環境でも `Changes` 見出し等の文字切れを防止。
- **カラー凡例（レジェンド）の右寄せ配置とヒューリスティック・ステータス表示**:
  - メインダイアログ下部（リストビュー直下）のレイアウト：
    - **右側**: カラー凡例テキスト（`● #7CE0AD: Isolated (Audio + Normal Routed)` `● #E09B97: Running (Scanning / Standby)`）を右詰めで描画。
    - **左側**: ヒューリスティック探索実行時の進行ターン（Phase）を英語で動的表示：
      - **Turn 1 (待機中)**: `Heuristics: Waiting for audio thread activity...`
      - **Turn 2 (特定中)**: `Heuristics: Identifying audio thread via delta...`
      - **Turn 3 (隔離開始)**: 隔離（iso）が開始されたら表示を消去（空白）。
- **再描画負荷抑制とフリッカー防止**:
  - `LVS_EX_DOUBLEBUFFER`（ダブルバッファリング）により、インジケーターの明滅パルス再描画時も画面チラつきを抑制。
- **操作の責務分離**:
  - メイン画面では直接アフィニティ編集を行わず、監視専用テーブルとする。
  - アフィニティおよび優先度の変更は、行ダブルクリック（`NM_DBLCLK`）または `[Edit (&E)]` ボタン押下で開く個別編集ダイアログ内で行う。
- **操作ボタン群**:
  - `[Always on Top]`: 最前面表示切り替えチェックボックス
  - `[Heuristics]`: 非MMCSSスレッド探索用ヒューリスティック監視切り替えチェックボックス（ON時のみCPU時間サンプリングによる再生スレッド自動特定を実行。OFF時は完全軽量化・負荷ゼロ）
  - `[To Tray (&T)]`: ウィンドウを非表示にし、システムトレイへ格納
  - `[Settings (&S)]`: 全体設定ダイアログを開く
  - `[Add (&A)]`: プロセス選択ダイアログを開く
  - `[Edit (&E)]`: 選択したプロセスの個別設定（CPU Core, Priority）ダイアログを開く
  - `[Remove (&R)]`: 選択したプロセスをリストから除外
  - `[Exit (&X)]`: アプリケーションを終了

### 2.2 プロセス選択ダイアログ (Process Picker Dialog)
- 実行中プロセス（`CreateToolhelp32Snapshot`）を一覧走査し、チェックボックス付き ListView で表示。
- カラム: `Process Name`, `Executable`, `Priority Class`（`LVSCW_AUTOSIZE_USEHEADER` 適用）。
- `_stricmp` による大文字小文字不問の自然なアルファベット順ソート。
- 選択して `[Done]` を押下すると、メイン画面の監視リストに追加（初期割当コア・優先度は全体デフォルト値）。

### 2.3 システムトレイ（タスクバー通知領域）動作
- ウィンドウの `[X]` 閉じるボタン、または `[Hide]` ボタンでシステムトレイに収納。
- **トレイアイコンのダブルクリック**: メイン設定画面を開き、最前面へ表示。
- **トレイアイコンの右クリック**: コンテキストメニュー表示（`Open Settings`, `Start with Windows`, `Exit`）。

### 2.4 単一インスタンス制御 (Single Instance)
- 名前付きミューテックス `Global\AudioThreadIsolator_SingleInstance` を用いて多重起動を物理的に防止。
- 既存起動プロセスがある場合は既存ウィンドウをフォアグラウンド表示し、後発プロセスは直ちに終了。

### 2.5 ATI 自身の自己隔離と低優先度常駐 (Self Isolation & Idle Priority)
- **自己アフィニティの通常コア固定**:
  起動時（`LoadConfig` 直後）に設定された `NormalCores` を自プロセスへ強制適用し、ATI 自身の監視処理がオーディオコアを踏むリスクを防止。
- **プロセス優先度の LOW 化 (`IDLE_PRIORITY_CLASS`)**:
  プロセス自体の基本優先度を最下位（BasePri 4）に設定し、CPU の空き時間のみで静粛に動作。

### 2.6 設定ダイアログと個別編集ダイアログ (表形式コア選択と右詰め適正配置)
- **表形式（ListView）によるコア選択の統一意匠**:
  - 上部に表（ListView）を配置。ヘッダーに CPU トポロジーに応じた動的コアラベル（`#0`, `#1`, `P0`, `E0` 等）を配置し、下段セル中央に正方形チェックボックスを描画。
  - **Global Settings**:
    - 行 0: `Default Audio Core`（単一選択チェックボックス）
    - 行 1: `Normal Cores`（複数選択チェックボックス）
  - **Rule Edit**:
    - 行 0: `Audio Core`（単一選択チェックボックス）
- **入力欄の右詰め・サイズ適正化**:
  - 過大な入力枠サイズを内容に見合った幅（数値欄 60 DBU、マスク欄 80 DBU、優先度コンボ 110 DBU）に縮小。
  - すべての入力コントロールの右端を整列させて右詰め配置。
  - 表のチェックボックス操作と右側エディットボックス（コア番号、マスク）はリアルタイムで双方向に連動。
- **CPU トポロジー判定と動的コアラベル命名**:
  - ハイブリッド CPU: `P0, P1...`、`E4, E5...`
  - 純Eコア CPU: `E0, E1, E2, E3...`
  - 均一 CPU: `#{i}`（`#0, #1, #2...`）

---

## 3. 内部ロジック・スレッド隔離仕様

### 3.1 監視ループ (設定されたポーリング間隔での更新)
- 500ms ごとにリスト内の登録プロセスを監視。
- **マルチプロセス対応 (Chromium / Vivaldi / Chrome 等)**:
  ブラウザはメインブラウザ、GPUプロセス、レンダラー、Audio Service（ユーティリティ）など複数の独立した子プロセスで動作する。
  そのため、単一 PID のみの走査ではなく、実行中の全同名プロセス（全 PID インスタンス）を走査する。
- **単一スナップショット最適化**:
  プロセスごとに毎回 `CreateToolhelp32Snapshot(TH32CS_SNAPTHREAD)` を発行せず、1 回のポーリングループ外でシステム全体のスレッドスナップショットを 1 回のみ取得し、プロセスIDごとのマップ（`processThreads[pid]`）に分類して走査することで、CPU 負荷を極小化する。
- **休眠タブ (IDLE_PRIORITY_CLASS) の走査スキップ**:
  非アクティブなタブは OS により `IDLE_PRIORITY_CLASS`（BasePri 5）に落とされており、オーディオスレッド（BasePri 15 または Idle）との競合リスクが極めて低いため、走査ループから自動除外してシステムコール発行負荷を大幅に削減する。なお、音声を出力する `Utility: Audio Service` や GPU プロセスはスキップ対象外（NORMAL / ABOVE_NORMAL）として維持される。

### 3.2 コアマスク制御（Windows カーネル制約の解決）
- **親プロセスマスクの拡張**:
  Windows カーネルの制約により「スレッドのアフィニティマスク ⊆ 親プロセスのマスク」である必要があるため、親プロセスには `通常コア群 ∪ 隔離コア`（例: Core 0, 1, 3, 4, 5 ➔ `0x3B`）を設定する。
- **再生スレッドの隔離と優先度制御**:
  特定されたオーディオ再生スレッドに対し、単独コア（例: Core 3 ➔ `0x08`）のアフィニティを適用し、指定された優先度（デフォルト: `THREAD_PRIORITY_IDLE` = -15 ➔ Base Priority 1）を設定。
  すでにスレッド優先度が目標値と一致している場合は余計な `SetThreadPriority` を呼び出さず、OS への負荷と競合を防止して安定化する。
- **通常スレッドの退避**:
  それ以外の全通常スレッドに対し、通常コア群（例: Core 0, 1, 4, 5 ➔ `0x33`）のアフィニティを適用。
- **スレッド規模 (Threads) と変更回数 (Changes) カウンタ**:
  - `Threads` 列: プロセス内に存在する全スレッド（アフィニティ制御対象）の総数を表示（例: `mpv.exe` の場合 `66`、`vivaldi.exe` の場合 `640+` などアプリ固有のスレッド規模を可視化）。
  - `Changes` 列: オーディオスレッドへの単独コア隔離・優先度適用が行われた回数のみをカウントアップ（通常スレッド退避によるカウンタ爆増を排除）。

### 3.3 再生スレッドの自動走査・特定判定エンジン
1. **第 1 判定 (スレッド名による明示一致)**:
   `GetThreadDescription` で取得したスレッド名が以下のパターンに一致するか判定：
   - `mpv.exe`: `ao/*` (例: `ao/wasapi`, `ao/pipewire`)
   - Chromium 系 (`chrome.exe`, `vivaldi.exe` 等):
     - `wasapi_render_thread`: 実際の WASAPI レンダリングを担当する主役スレッド（Audio Service プロセス内）。
     - `AudioOutputDevice`: レンダラー側の音声出力スレッド。
     - `AudioThread`, `CrAudio`: Chromium 内部オーディオスレッド。
     - **除外フィルタ**: `audio.CrUtilityMain` や `watchdog` 等の制御・監視メインスレッドは誤検出を防止するため明示的に除外。
   - 一般オーディオキーワード: `audio`, `playback`, `wasapi`, `asio`, `fmod`, `sound`, `mixer`
   - **動的生成スレッドの追従**:
     Chromium の音声レンダリングスレッド（`wasapi_render_thread`）は、動画・音声の再生中のみ動的に生成され、再生停止・終了時に自動破棄される。ATI はこれをリアルタイムに検知し、生成された瞬間に即時単独コア隔離と優先度設定を適用する。
2. **第 2 判定 (MMCSS リアルタイム帯判定 - Godot / Luck be a Landlord 等)**:
   - スレッド名を持たず、Base Priority が 15 以上（MMCSS / ASIO 帯）のスレッド候補を抽出。
   - プロセス内に該当する無名スレッドが **「ちょうど 1 本」** の場合、誤判定リスクなく `MMCSS (TID: xxxxx)` として自動認定。
3. **第 3 判定 (非 MMCSS ゲーム CPU サンプリング判定 - Unity / DungeonClawler 等)**:
   - メイン画面の `Heuristics` チェックボックスが ON の場合のみ発火（OFF 時はサンプリング完全スキップで負荷ゼロ）。
   - **対象モジュールの限定**: スレッド開始アドレスがゲーム本体 exe または `UnityPlayer.dll` に属するスレッドのみを対象とし、外部入力 DLL（`Windows.Gaming.Input.dll`）やドライバ DLL を除外。
   - **過大デルタ除外と適正ポーリング特定**:
     - MMCSS スレッドが存在せず、Base Priority が 10（`THREAD_PRIORITY_HIGHEST`、Unity の音声ミキサー帯）のスレッド候補が存在する場合、`GetThreadTimes` を用いて CPU 時間デルタをサンプリング。
     - メインループや描画スレッド等の過大 delta スレッドを除外。
     - 10ms〜15.4ms ポーリング帯（500ms サイクルあたり 2ms〜15.4ms 程度の適正 delta）で定常稼働しているスレッドを特定。
     - 進行状況に応じて下部ステータス表示（Turn 1: `Heuristics: Waiting for audio thread activity...` ➔ Turn 2: `Heuristics: Identifying audio thread via delta...` ➔ Turn 3: 隔離開始・消去）を更新。
4. **永続オーディオスレッドトラッキング機構 (ピンポン現象解消とサンプリング完全停止)**:
   - 一度オーディオスレッドとして認定した TID は、プロセスIDごとに `m_trackedAudioThreads[pid][tid]` に永続記憶。
   - 優先度を Idle (-15) に降格した後も、生存中は O(1) で即座にオーディオスレッドとして継続追従・隔離（判定ピンポン現象の完全防止）。
   - **CPU サンプリングの完全停止**: 一度特定された後は、ヒューリスティックによる `GetThreadTimes` の発行自体が完全スキップされるため、ATI の CPU Usage は常に 0.00% を維持。プロセス終了またはスレッド破棄時に自動クリーンアップされる。

### 3.4 ウィンドウレイアウト・最前面表示・リサイズ・右詰め入力枠仕様
1. **ウィンドウの自由伸縮 (リサイズ対応)**:
   - メインダイアログ (`IDD_MAIN_DIALOG`)、全体設定ダイアログ (`IDD_SETTINGS_DIALOG`)、および個別編集ダイアログ (`IDD_RULE_EDIT_DIALOG`) は `WS_THICKFRAME` を持ち、ウィンドウ枠を掴んで自由に縦横伸縮可能。
   - `WM_SIZE` により、リストビュー（プロセス表、コア表）が幅・高さに自動追従し、右側ボタン群および右下ボタン（Exit, OK/Cancel）が適正位置にアンカー追従する。
   - `WM_GETMINMAXINFO` により最小ウィンドウサイズ（メイン: 485x300、設定: 380x205、個別編集: 280x215）が制限され、レイアウト破綻を防止。
   - 個別編集ダイアログ (`IDD_RULE_EDIT_DIALOG`) のタイトルは、実態（スレッド単位のアフィニティ・優先度設定）に合わせて **`Thread Isolation Settings`** に設定。
2. **メイン画面のゼブラストライプ（縞模様）交互背景色仕様**:
   - リストビューの各行の視認性を高めるため、偶数行と奇数行で背景色を交互に描画。
   - **奇数行（1, 3, 5...）**: 目に優しいソフトホワイト **`RGB(248, 248, 248)`**（`#F8F8F8`）
   - **偶数行（2, 4, 6...）**: 落ち着いたウォームグレー **`RGB(233, 231, 227)`**（`#E9E7E3`）
   - **選択行**: システム標準ハイライト色（青）
   - Col 0（チェックボックス／丸印）および Col 3（`Other than`）のカスタムドロー領域も、この行背景色と完全に連動させて描画。
3. **コア設定表（ListView）の 2 行ジャスト化（26 DBU）と表記統一**:
   - 全体設定ダイアログ (`IDD_SETTINGS_DIALOG`) および個別設定ダイアログ (`IDD_RULE_EDIT_DIALOG`) のコア表高さを 48 DBU ➔ **26 DBU** に縮小。
   - ヘッダー（約 9 DBU）＋ データ行 2 行（約 8.5 DBU × 2）がジャストで収まり、余分な空行（グリッド線）の表示を解消。
   - 表の行構成：
     - **行 0: `Audio Core`**（オーディオスレッドが**使う**コアにチェック）
     - **行 1: `Other than Audio`**（オーディオスレッド以外の全スレッドが**使う**退避先コアにチェック、従来の `Normal Cores` から統一）
4. **入力項目ラベル・テキストボックスの更新**:
   - `Audio Core(s) (#, ...):` / `Default Audio Core(s) (#, ...):`（カンマ区切りによる複数コア指定が可能なことを明示し、テキスト枠幅を 36 DBU に拡大）。
   - `Other than Audio (Mask):`（個別設定ダイアログに新設、全体設定の表記も統一。表のチェックとリアルタイムに相互連動する HEX 入力枠）。
5. **ダイアログ全体の高さ縮小**:
   - `Global Settings`: 全体高さを 174 DBU ➔ **152 DBU**（-22 DBU 縮小）。
   - `Thread Isolation Settings`: 全体高さを **164 DBU** にスリム化。
6. **Half-Isolated 動作に関する注釈テキスト配置**:
   - 個別設定ダイアログの入力枠下部に、Segoe UI 8pt の小さめフォントで以下の注釈を配置。
     > `Note: If other threads return to the audio core, status becomes Half-Isolated.`  
     > `Audio priority is auto-promoted by 1 step, keeping intruder threads 1 level below it.`
7. **プロセス個別退避先コア（Other than Audio Mask）の保持と適用**:
   - `ProcessRule.normalAffinityMask` により、プロセス個別の通常スレッド退避先コアマスクを設定可能。
   - `settings.ini` の `[Processes]` セクションに `NormalCores:0x...` として保存・読み込み（未指定時は Global の `NormalCores` を自動継承）。
   - スレッド走査時、個別設定がある場合は全体設定に優先してその個別退避先コアマスクを適用。
8. **メインダイアログのボタン高さ 2/3 化（14 ➔ 10 DBU）およびウィンドウ超コンパクト化（112 DBU）と重なり防止・凡例左寄せ仕様**:
   - **ボタン高さの 2/3 化**: 右側ボタン群（To Tray, Settings, Add, Edit, Remove, Exit）の高さを従来の 14 DBU から約 2/3 である **`10 DBU`**（200% DPI 時 40px）にスリム化。
   - **ウィンドウ高さの超コンパクト化**: 初回起動時のダイアログ高さを従来の 249 DBU から大幅にスリム化した **`112 DBU`**（200% DPI 時 Client: 448px, Window: 519px）に設定。
   - **Remove と Exit ボタンの重なり防止ガード**: `WM_SIZE` において、Exit ボタンの上端が Remove ボタンの下端より上へ移動しないよう `minExitY = rcRem.bottom + 6 * scale` の安全ガードを常時適用。初期配置でも 28px（7 DBU）の適正な余白を維持。
   - **横スクロールバーの解消と右側コントロール重なり防止ジャスト幅フィット**: ダイアログ初期幅を 470 DBU、右側コントロール領域幅 `rightColW` を **`105 * scale`** に維持し、`WM_INITDIALOG` において全 8 カラム合計幅（1272px）と右側コントロール幅から必要クライアント幅を算出して自動フィット。`Always on Top` との間に 15px の余白を保ちつつ、全カラム（`!` 〜 `Changes`）が横スクロールバーなしでぴったり収まるレイアウトを実現。
   - **下部凡例の左寄せ配置**: `WM_PAINT` において、3 つのカラー凡例（`Half-Isolated`, `Fully Isolated`, `Standby / Scanning`）の描画開始位置を `rcList.left + 4 * scale`（左寄せ）とし、右側の Exit ボタン下部へのめり込みを解消。

### 3.5 オーディオコアの複数コア（2コア以上）アフィニティ仕様
1. **複数コア指定**:
   - 負荷の高いオーディオ処理や音切れ発生時の負荷分散に対応するため、オーディオコア（`AudioCore`）に 2 コア以上の複数コア（例: `1, 2`）を指定可能。
   - コア表の該当コア列のチェックボックスを複数チェックするか、入力欄にカンマ区切り（例: `1, 2`）で直接入力可能。
2. **スレッド分離動作**:
   - オーディオスレッドには指定された複数コアのビット論理和マスク（`audioMask`）を適用。
   - 通常スレッドの退避先マスクからは、指定されたオーディオコア群のすべてが完全に除外される（`baseNormal & ~audioMask`）。
   - メイン画面の `CPU Core` 列には複数コアラベル（例: `P1,P2`、`#1,#2`）が表示される。

### 3.6 オーディオ専有コア侵入・同居スレッドの連動優先度制御および動的パルス表示仕様
1. **侵入・同居スレッドの検知**:
   - オーディオスレッド特定後、オーディオスレッド以外のスレッドで `audioMask`（オーディオ専有コア）をアフィニティに持つスレッド（NVIDIA GPU ドライバの自己バインドスレッド `nvwgf2umx.dll!NVDEV_Thunk` 等）を毎ポーリング周期で自動検出。
2. **優先度の連動階層制御 (直下昇格・降格)**:
   - 侵入・同居スレッド（複数可）の優先度は、常に「オーディオスレッドの優先度の 1 段階下（直下階層）」に連動して自動設定・維持される。
   - Win32 API 優先度連動マッピング：
     - オーディオ `+15` (`TIME_CRITICAL`) ➔ 同居スレッド `+2` (`HIGHEST`)
     - オーディオ `+2` (`HIGHEST`) ➔ 同居スレッド `+1` (`ABOVE_NORMAL`)
     - オーディオ `+1` (`ABOVE_NORMAL`) ➔ 同居スレッド `0` (`NORMAL`)
     - オーディオ `0` (`NORMAL`) ➔ 同居スレッド `-1` (`BELOW_NORMAL`)
     - オーディオ `-1` (`BELOW_NORMAL`) ➔ 同居スレッド `-2` (`LOWEST`)
     - オーディオ `-2` (`LOWEST`) ※基本状態 ➔ 同居スレッド `-15` (`IDLE`)
     - オーディオ `-15` (`IDLE`) ➔ 同居スレッド `-15` (`IDLE`)
   - これにより、Core #5 上で同居が発生しても常に「オーディオ ＞ 同居スレッド」の優先関係が担保され、ユーザーが処理能力底上げのためにオーディオ優先度を昇格させた場合も同居スレッドが直下階層へ連動追従する。
3. **インジケーター丸印（●）の動的パルス表示**:
   - **通常の隔離完了時**: 青緑 `#7CE0AD`（RGB 124, 224, 173）の 10% 明滅パルス。
   - **侵入・同居スレッド制圧中**: 鈍い黄色（RGB 216, 178, 76）と 現行の青緑（RGB 124, 224, 173）が、ポーリング周期ごとにパカパカ交互点滅。一目でオーディオ専有コアへの同居侵入・制圧状態を把握可能。
   - **未特定時（Scanning / Standby）**: コーラルピンク `#E09B97`（RGB 224, 155, 151）の 10% 明滅パルス。

### 3.7 下部2段レイアウト（3種凡例・ステータス表示）およびプロセスEDITダイアログ直上配置仕様
1. **下部2段レイアウト (ステータス表示の1行下移動)**:
   - 下部領域を 2 行構成に分離し、凡例ブロックとヒューリスティック探索ステータス（`Heuristics: ...`）の重なりを解消。
   - ウィンドウ初期高さ（440x249 DBU）および最小ウィンドウ高さ（`ptMinTrackSize.y = 300 * scale`）を 1 行分拡張。
2. **3種カラー凡例の並び順・配色・右寄せ配置 (1行目)**:
   - **`● Half-Isolated (Audio<-OtherThread)`**（1番目）: 表内と同じく、鈍い黄色（RGB 216, 178, 76）と青緑（RGB 124, 224, 173）がポーリング周波数でパカパカ交互点滅。
   - **`● Fully Isolated`**（2番目）: 青緑 `#7CE0AD`（RGB 124, 224, 173）
   - **`● Standby / Scanning`**（3番目）: コーラルピンク `#E09B97`（RGB 224, 155, 151）
3. **ヒューリスティック進行ステータス表示 (2行目)**:
   - 凡例のもう 1 行下（`startY + 16 * scale`）に左寄せで表示。探索中のターン情報（Turn 1 ➔ Turn 2 ➔ 隔離完了時消去）が凡例と独立して明瞭に視認可能。
4. **プロセスEDIT画面の直上配置仕様**:
   - `RuleEditDlgProc` 起動時、画面中央固定（`DS_CENTER`）を排し、メイン画面のリストビューで選択された当該プロセス行のスクリーン矩形を取得。
   - 当該プロセス行を覆い隠さない直上（`rcItem.top - dlgH - 4`）にダイアログを自動配置（モニター上端からはみ出る場合は直下 `rcItem.bottom + 4` へ反転配置）。
   - これにより、ユーザーはメイン画面のプロセス行情報（コア番号やスレッド状態）を見ながらダイアログで直感的に編集が可能。

### 3.8 Col 0 監視除外（Bypass / Ignore）および赤太字 '!' ヘッダー仕様
1. **赤太字 '!' ヘッダー (ヘッダーカスタムドロー)**:
   - 最左端 Col 0 のカラム見出しテキストを **`!`** とし、ヘッダーの `NM_CUSTOMDRAW` により赤色（RGB 220, 50, 50）かつ Bold（太字）で描画。
   - 「チェックを入れると除外（NOT / Bypass）」する機能性を視覚的に明示。
2. **全行チェックボックス表示 (起動中・停止中問わず)**:
   - 停止中（未起動）のプロセス行も含め、全行の先頭にチェックボックス（`DFCS_BUTTONCHECK`）を描画。
   - 停止中プロセスであっても、ユーザーは事前にチェックを入れて「起動しても監視しない（ペンディング）」状態に設定可能。
3. **チェックON（監視除外 / Bypass）の挙動**:
   - 当該プロセスのスレッド走査、既知スレッド名一致、MMCSS判定、Adopt判定、ヒューリスティック探索、通常スレッドアフィニティ退避制御を**完全にスキップ**。
   - スレッドのアフィニティや優先度は一切変更されず、OS本来の挙動が維持される。
   - リストビューの丸印インジケーター（●）は非表示となり、`Other than` カラムは `-` と表示される。
4. **起動時インジケーター並置**:
   - チェックOFF（通常監視）時のみ、起動中にチェックボックスの右隣へ丸印インジケーター（青緑 / 黄色パカパカ点滅 / ピンク）を描画。
   - 隔離完了後であっても、ユーザーがチェックボックスをクリックすれば即座に監視除外へ切り替え可能。
5. **設定の永続化**:
   - `settings.ini` の `[Processes]` セクションにおいて、`Bypass:1` として除外状態を永続化。

### 3.9 FMOD / Unity スタック底スレッド名走査エンジンおよびオーディオスレッド適性ランク判定仕様
1. **スタック底内部構造体走査エンジン (`QueryFmodOrUnityThreadName`)**:
   - Unity (FMOD サウンドエンジン) 等のゲームプロセスでは、Windows の `SetThreadDescription` API が呼ばれず、OS標準APIではスレッド名が空文字となる。
   - スレッドの TEB から `StackBase`（スタック底）を取得し、スタック底直下 512 バイト（`[StackBase - 0x200, StackBase)`）のメモリ領域を `ReadProcessMemory` で走査。
   - スタック上に退避された内部スレッド管理構造体（`ThreadData*`）およびその引数領域（`ArgPtr`）を1バイト刻みで走査し、平文で格納されている FMOD スレッド名文字列（`"FMOD (WASAPI) feeder thread"`, `"FMOD mixer thread"` 等）を直接抽出・特定。
2. **オーディオスレッド適性ランク判定 (`GetAudioThreadRank`)**:
   - 単一のプロセス内に複数のオーディオ関連スレッド（Mixer, Feeder, Stream 等）が存在する場合、CPU delta による曖昧な推測を排し、役割に応じた明確な優先度ランクを適用：
     - **Rank 100 (最優先・本命隔離対象)**: `"feeder"`（`FMOD (WASAPI) feeder thread` 等）- WASAPI ハードウェアバッファへ直接 PCM を供給する最重要リアルタイムスレッド。
      - **Rank 95 - 80 (WASAPI / ASIO / Playback DAC出力最前線)**:
        - **Rank 95**: `"wasapi"`（foobar2000 WASAPI 排他ワーカー `"WASAPI Exclusive Worker"`, Chromium `"wasapi_render_thread"`, mpv `"ao/wasapi"` 等）
        - **Rank 92**: `"ao"` (mpv オーディオ出力スレッド)
        - **Rank 90**: `"asio"` (ASIO 出力スレッド)
        - **Rank 85**: `"playback"`（foobar2000 再生管理スレッド `"Fb2k Playback Thread"` 等、デコードを除く再生制御スレッド）
        - **Rank 80**: `"audiooutputdevice"`
     - **Rank 65**: `"audiothread"`, `"craudio"`
     - **Rank 50**: `"mixer"` (`FMOD mixer thread` - 波形演算)
     - **Rank 40**: `"audio"`, `"playback"`, `"sound"`
     - **Rank 35**: `"decod"` (`Fb2k Playback Decoding Thread` 等のデコードスレッド)
     - **Rank 20 - 10 (ゲーム系)**: `"stream"` (`FMOD stream thread` - BGMデコード), `"fmod"`
     - **Rank 0 (完全除外)**: `"file thread"` (ディスクI/O), `"utility"`, `"main"`, `"watchdog"`
3. **動的すげ替え・昇格仕様**:
   - 既にスレッドが検出・隔離されている場合でも、走査によってより高ランクなスレッド（例: Mixer よりも高位の Feeder、あるいはデコードよりも高位の Playback Thread）が発見された場合、自動的にトラッキング情報をクリア・更新し、真の最優先オーディオスレッドへライブですげ替えを実施。

### 3.11 メディアプレーヤー（foobar2000 等）の優先度条件解除およびスレッド名最優先検出仕様
1. **プロセス優先度クラスによる除外のブラウザ系限定化**:
   - Chromium 系ブラウザ（`chrome.exe`, `vivaldi.exe`, `msedge.exe`, `brave.exe`, `opera.exe`）では、大量の休眠バックグラウンドタブが OS によって `IDLE_PRIORITY_CLASS` に設定されるため、走査負荷削減のためにアイドル優先度クラスのプロセスをスキップする。
   - `foobar2000.exe` 等のメディアプレーヤーや通常アプリは、音質・低負荷設定等によってプロセス自体が `IDLE_PRIORITY_CLASS` で動作することがあるため、この除外判定をブラウザ系プロセスに限定。スタンドアロンのメディアプレーヤーやゲームではプロセスの優先度クラスに関わらず確実にオープン・走査を実施。
2. **名前付きスレッドの優先度条件免除と最優先検出**:
   - `"wasapi"`, `"playback"`（foobar2000 の `Fb2k Playback Thread` 等）, `"asio"` 等の名前を持つスレッドが存在する場合、BasePri や Priority などの優先度条件を問わず、スレッド名判定（Rank 80〜95）により真っ先にオーディオスレッドとして検出・隔離。
   - デコードスレッド（`decod`）は Rank 35 とし、実際の DAC 再生出力を担当する `Playback Thread` を最上位（Rank 90）として確実に選定。
   - ゲーム系（FMOD、Unity、無名スレッド判定、MMCSS帯、ポーリングヒューリスティクス等）は従来の検出ロジックをそのまま継続。

### 3.10 Col 0 チェックボックス/丸印インジケーター統合切り替えおよび先行全スレッド退避・Other than '!' 赤文字仕様
1. **Col 0 ヘッダーの赤太字単独描画 (二重表示防止)**:
   - `ListView_InsertColumn` の初期見出しテキストを空文字（`""`）に設定し、Windows 標準の黒文字描画を抑止。
   - `SysHeader32` をサブクラス化（`CustomHeaderProc`）し、`WM_PAINT` 時にヘッダー背景色（`COLOR_BTNFACE`）で背景をクリアした上で、鮮やかな赤色（RGB 220, 50, 50）かつ太字（`FW_BOLD`）のフォントで **`!`** のみを中央描画。
2. **Col 0 の状態別統合切り替え表示 (コンパクト幅 24*scale 維持)**:
   - チェックボックスと丸印インジケーターを別カラムや横並びにせず、Col 0 セルの中央（`(cellW - size) / 2`）に状態に応じて切り替え描画：
     - **未起動時（`!r.isRunning`）**:
       - 通常監視: **`☐`**（空チェックボックス、事前除外設定用）。クリックで除外へトグル可能。
       - 監視除外: **`☑`**（チェック入りチェックボックス）。クリックで解除へトグル可能。
      - **起動中かつ監視除外（`r.isRunning && r.isBypassed`）**:
        - **`☑`**（チェック入りチェックボックス）。ATI によるスレッド変更が一切行われていない未介入状態であるため、**クリックによる監視除外の解除（通常監視・アイソレート移行）を許可**。解除時は直ちに `ScanAndIsolate` を即時実行してスレッド隔離・優先度制御を適用。
     - **起動中かつ監視通常（`r.isRunning && !r.isBypassed`）**:
       - **チェックボックスの場所に丸印インジケーター（`●`）を描画**（稼働中の優先度保護のためクリック無効化）：
         - 侵入スレッド制圧中（Half-Isolated）: 鈍い黄色（RGB 216, 178, 76）と青緑（RGB 124, 224, 173）の交互点滅。
         - 通常隔離完了（Fully Isolated）: 青緑 `#7CE0AD`（RGB 124, 224, 173）。
         - 未特定（Standby / Scanning）: コーラルピンク `#E09B97`（RGB 224, 155, 151）。
3. **オーディオスレッド確定前の先行全スレッド退避 (`src/isolator.cpp`)**:
   - プロセス起動時、オーディオスレッドが未確定（特定待ち）であっても、`targetMask = normalMask`（オーディオコアを除外した通常コア群）を適用し、プロセス内の全スレッドを直ちに通常コア群へ先行退避。
   - オーディオ専有コア（Core #5 等）を事前に空けておくことで、初期化スレッド等によるコア汚染を未然に防止。
4. **Other than 列（Col 3）退避稼働時の '!' 赤文字描画**:
   - 状態が「起動中かつ監視通常（`r.isRunning && !r.isBypassed`）」のプロセスにおいて、特定前の先行退避中（Scanning）および隔離完了後（Fully Isolated / Half-Isolated）のいずれであっても、Other than の全スレッドはオーディオコアから退避済みであるため、Other than 列テキスト（例: `!#1,5`）の先頭 **`!`** のみ文字色を鮮やかな赤色（RGB 220, 50, 50）かつ太字で描画。
   - 「オーディオ専有コアから確実に除外（NOT）退避されている状態」を直感的に明示。
5. **優先度クイックアクセス (インプレース ComboBox)**:
   - メイン画面の固定 8 列監視テーブルにおいて、Priority 列（Col 5）のセルをクリックした際、その場にドロップダウンリスト（ComboBox）を即時展開。
   - ListView のサブクラス化（`CustomListProc`）により `WM_LBUTTONDOWN` を捕捉して展開し、ListView 標準の行選択や不要な再クリック判定を抑止。
   - 選択肢（`Idle (-15)`、`Lowest (-2)`、`Below Normal (-1)`、`Normal (0)`、`Above Normal (+1)`、`Highest (+2)`、`Time Critical (+15)`）から希望の優先度を選択可能。
   - 項目選択時（`CBN_SELCHANGE`）および選択確定時（`CBN_SELENDOK`）に、対象プロセスのルールを即時更新し、`settings.ini` への保存および実行中スレッドへの優先度再適用（`ScanAndIsolate`）を同期的に実行。
   - キャンセル時（`CBN_SELENDCANCEL`）は変更を破棄し、ドロップダウンクローズ時（`CBN_CLOSEUP`）での意図しない初期値への巻き戻りを抑止。
   - ドロップダウンクローズ時、画面スクロール時、リサイズ時、他セルクリック時は自動的に非表示化され、通常のリスト表示へ復帰。
6. **Process Name カラム幅の 10% 拡大**:
   - プロセス名表示の視認性向上のため、Process Name 列（Col 1）のカラム幅を `105 * scale` から 10% 拡大した **`116 * scale`** に拡張設定。
   - ヘッダー自動調整ループ（`LVSCW_AUTOSIZE_USEHEADER`）においても固定幅除外リスト（Col 0, 1, 5, 7）に登録し、拡大幅を維持。
7. **ボタン高さ 2/3 化およびウィンドウ超コンパクト化（112 DBU）・重なり防止・横スクロールバー解消**:
   - 右側ボタン群（To Tray, Settings, Add, Edit, Remove, Exit）の高さを従来の 14 DBU から約 2/3 の **`10 DBU`**（200% DPI で 40px）にスリム化。
   - メインダイアログ高さを **`112 DBU`**（200% DPI で Window: 519px, Client: 448px）へ超コンパクト化。
   - ウィンドウ最小トラッキング高さ（`ptMinTrackSize.y`）を `180 * scale`（360px）に設定。
   - リサイズ時に Exit ボタンが Remove ボタン下端に重ならないよう、`minExitY = rcRem.bottom + 6 * scale` の重なり防止ガードを実装（28px の安全余白を確保）。
8. **表の右側コントロール衝突解消および下部カラー凡例の左寄せ配置**:
   - 表（ListView）が右側コントロール群（`Always on Top` チェックボックス等）と重なる現象を防止するため、右側コントロール領域幅 `rightColW` を適正な **`105 * scale`**（210px）に維持。
   - リソース定義のダイアログ幅を **`470 DBU`**（初期リストビュー幅 388 DBU、右側コントロール X=404 DBU）に設定し、`WM_INITDIALOG` で全 8 カラム合計幅（1272px）とジャストフィット連動させ、横スクロールバー（`WS_HSCROLL`）を非表示化。
   - メイン画面下部の 3 つのカラー凡例（`Half-Isolated`, `Fully Isolated`, `Standby / Scanning`）を、表の左端に合わせた **`rcList.left + 4 * scale` 起点の左寄せ配置**に変更し、右側の Exit ボタン真下へのめり込み・テキスト欠落を解消。

### 3.12 Godot WASAPI コールスタックシグネチャ走査およびメインスレッド事前除外仕様
1. **メインスレッド（第1スレッド）の構造的完全除外**:
   - プロセス生成時に最初に作られる第1スレッド（スナップショット先頭スレッド `ptIt->second[0].th32ThreadID`）は、ウィンドウメッセージループやゲームロジックを担当するメインスレッドである。
   - ゲーム起動時、OSの描画ブーストや Games タスクによりメインスレッドの `BasePri` が一時的に 15〜19（リアルタイム帯）へ引き上げられる場合がある。
   - MMCSS判定、Adopt判定、ヒューリスティック探索の全経路において `ti.tid == mainThreadTid` を事前に除外し、メインスレッドがオーディオ枠に誤確定されることを構造的に完全防止。
2. **コールスタック参照シグネチャ走査エンジン (`QueryCallstackAudioSignature`)**:
   - Godot Engine 等のゲームエンジンではスレッド名変数を持たず、OS標準API（`SetThreadDescription`）も呼ばれないため、無名スレッドとして生成される。
   - スレッドの TEB（`NtQueryInformationThread` の `ThreadBasicInformation`）から `StackBase` を取得し、スタック領域（StackBase 手前 4KB）を走査。
   - スタックに積まれたリターンアドレスから関数コード近傍（`[rip + disp32]` の相対参照等）を走査し、`"audio_driver_wasapi"`, `"AudioDriverWASAPI"`, `"AudioServer"` の文字列参照を直接検出。
   - 検出時は即座にスレッド名 `"WASAPI (Godot AudioDriver)"`（Rank 95）を自動付与し、名前一致判定により初回 1 ターン目で即時特定・隔離。
3. **個別モジュール依存の Rank / ホワイトリスト追加の廃止**:
   - 狭い個別 DLL（`dsound.dll`, `winmm.dll`, `msvcrt.dll` 等）に依存した Rank 追加やホワイトリスト追加を排し、安全なメインスレッド除外ガードおよびコールスタック上の決定打コードシグネチャ走査によって真のオーディオスレッドを特定する汎用設計を維持。

### 3.13 10秒サンプリング・最上 delta 除外・2〜10位候補検査および真の侵入者連動昇格・降格仕様
1. **モジュールホワイトリストの全廃**:
   - ゲーム本体および `unityplayer.dll` 以外を一律門前払いするホワイトリストを完全撤廃。
   - `DSOUND.dll`、`WINMM.dll`、`pxtoneWin32.dll` 等の外部サウンドモジュールを含むプロセス内の全スレッドを探索・サンプリング対象とする。
2. **10秒間サンプリング・最上 delta 除外・2〜10位候補検査エンジン**:
   - ヒューリスティック有効時、全スレッドの CPU 時間（`GetThreadTimes`）を 500ms ごとに 20 ターン（約 10 秒間）サンプリング集計。
   - サンプリング観察期間中は画面ステータスに `Heuristics: Sampling (X.X/10.0s)...` を表示し、全スレッドを安全に通常コア群へ先行退避（優先度変更なし）。
   - 10秒経過後、累積 delta の降順ランキングを作成。
   - **第 1 位（最上 delta スレッド：メイン描画ループ等）を候補から構造的に除外**。
   - **第 2 位 〜 第 10 位のスレッドを候補群として抽出**し、オーディオモジュール判定（`dsound`, `winmm`, `pxtone` 等）または適正ポーリング帯検査（20ターン累計 100,000〜5,000,000 ticks）により本命オーディオスレッド（`TID 26868` 等）を自動確定・隔離。
3. **退避後物理確認による「真の侵入者」検出および通常スレッド巻き込み降格の根絶**:
   - 通常スレッドを通常コア群（`normalMask`）へ退避させた後、`QueryThreadAffinityMask` で実際のアフィニティを物理確認。
   - 退避処理後もなお Core #5（`audioMask`）を含んでいるスレッド（ドライバ等による自己バインド居座りスレッド）のみを「真の侵入者」として検出。
   - 正常に通常コアへ退避できたメインスレッド・入力スレッド・描画スレッド等の優先度には一切触れず、ゲーム本来の優先度を 100% 維持。
4. **同居スレッド制圧時のオーディオ優先度 1段階昇格および侵入者直下階層降格**:
   - 真の侵入者が検出された場合（Half-Isolated）：
     - **オーディオスレッド**: 設定優先度が `-15`（`THREAD_PRIORITY_IDLE`）なら、同居相手との優先差を保つため **`-2`（`THREAD_PRIORITY_LOWEST`）に 1 段階昇格**。設定優先度が `-2` 以上（例: `0`）ならその値を維持。
     - **侵入者スレッド**: オーディオスレッドの適用優先度の **直下階層（1段階下）** に設定。
       - オーディオが `-2`（昇格後）➔ 侵入者は `-15`（IDLE）
       - オーディオが `0`（NORMAL）➔ 侵入者は `-1`（BELOW_NORMAL）
       - オーディオが `+1`（ABOVE_NORMAL）➔ 侵入者は `0`（NORMAL）
       - オーディオが `+2`（HIGHEST）➔ 侵入者は `+1`（ABOVE_NORMAL）

---

## 4. 設定永続化仕様 (settings.ini)

実行ファイルと同階層の `settings.ini` に保存・読み込み。
初回起動時はハードウェアの論理コア数を自動取得し、クリーンな初期値で生成される（登録プロセスは空）。

```ini
[Global]
; 初回自動検出: 2コア以上で #1、NormalCores は Core #1 を除外したマスク（例: 6コアなら 0x3D、4コアなら 0x0D、2コアなら 0x01）
DefaultAudioCore = 1
; 複数コア指定例: DefaultAudioCore = 1, 2
DefaultAudioPriority = -15
NormalCores = 0x3D
PollingIntervalMs = 500
AlwaysOnTop = 0

[Processes]
; フォーマット: <exe名> = AudioCore:<core>, AudioPriority:<priority>[, Bypass:1]
; 単一コア例: mpv.exe = AudioCore:1, AudioPriority:-15
; 複数コア例: mpv.exe = AudioCore:1, 2, AudioPriority:-15
; 監視除外例: mpv.exe = AudioCore:5, AudioPriority:-2, Bypass:1
```



